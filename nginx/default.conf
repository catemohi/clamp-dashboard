upstream django {
    server webapp:8000;
}

server {
    listen 80;

    location / {
        proxy_pass http://webapp:8000;
        proxy_set_header X-Forwarded-Proto https;
    }

    location /static/ {
        alias /static/;
    }
}